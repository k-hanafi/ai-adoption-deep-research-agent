# March snapshot inputs (local only)

The licensed Crunchbase copies that used to live here are gitignored. Drop
them back in this folder if you need to rerun the March agent.

Schema samples for the live repo live at `../../crunchbase_data/sample/`.
