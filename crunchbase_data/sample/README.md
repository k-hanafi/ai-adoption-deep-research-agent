# Fictional schema samples

These rows are invented. They are not Crunchbase records.

| File | Matches |
|---|---|
| `startups.sample.csv` | Stage 1 input columns |
| `stage2_input.sample.jsonl` | Production / Stage 2 queue (priority 4–5 shape) |

Put the real licensed dumps one folder up, using the filenames in
`crunchbase_data/README.md`.
