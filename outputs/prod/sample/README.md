# Production output sample

Fictional rows that match the live `findings.csv` columns. Real production
writes stay under `outputs/prod/{sgs,pcs,uas}/` and are gitignored.

A no-finding company is included so the empty-result shape is visible.
